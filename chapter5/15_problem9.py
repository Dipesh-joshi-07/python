""""Can you change the values inside a list which is contained in set S?
s = {8, 7, 12, "Harry", [1,2]}"""

s = {8, 7, 12, "Harry", [1,2]}
print(s)

    # no we cant.first, we cannt include list in set
    # second we cant change the value by indexing

